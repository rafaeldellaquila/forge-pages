import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Note: `next dev` regenerates the root AGENTS.md/CLAUDE.md on every run
  // (agentRules). Set `agentRules: false` to stop it.
}

export default nextConfig
