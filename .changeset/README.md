# Changesets

This folder is used by `@changesets/cli` to version packages and generate changelogs.

Create a new changeset with `mise run changeset` (or `pnpm changeset`).
See the [Changesets docs](https://github.com/changesets/changesets) for details.
