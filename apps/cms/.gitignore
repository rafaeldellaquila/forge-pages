# Strapi — generated and runtime artifacts (never commit)
.env
.cache
.tmp
build
dist
.strapi
.strapi-updater.json
exports
*.db
license.txt

# Generated TypeScript types (regenerated on every build)
types/generated

# Uploaded media (local disk fallback)
public/uploads/*
!public/uploads/.gitkeep
