{
  "collectionName": "components_blocks_headers",
  "info": {
    "displayName": "Header",
    "icon": "layer",
    "description": "Fixed navigation header with logo and CTA"
  },
  "options": {},
  "attributes": {
    "variant": {
      "type": "enumeration",
      "enum": ["default", "centered"],
      "default": "default"
    },
    "background": { "type": "component", "component": "shared.background" },
    "logo": { "type": "media", "multiple": false, "allowedTypes": ["images"] },
    "menuLinks": { "type": "component", "repeatable": true, "component": "shared.link" },
    "ctaLabel": { "type": "string", "required": true },
    "ctaWhatsapp": { "type": "string" },
    "ctaMessage": { "type": "string" },
    "ctaLink": { "type": "string" }
  }
}
