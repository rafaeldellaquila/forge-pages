name: Deploy — Cloudflare Workers

# Deploys the Next.js app to Cloudflare Workers via the OpenNext adapter
# (see docs/adr/0001-hosting-cloudflare.md).
# Activate by uncommenting the push trigger. Requires these repo secrets:
#   CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID  (+ the app's runtime secrets, see SETUP.md)
# on:
#   push:
#     branches: [main]

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Build & deploy to Cloudflare Workers
    runs-on: ubuntu-latest

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      NEXT_PUBLIC_TURNSTILE_SITE_KEY: ${{ secrets.NEXT_PUBLIC_TURNSTILE_SITE_KEY }}
      TURNSTILE_SECRET_KEY: ${{ secrets.TURNSTILE_SECRET_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup mise
        uses: jdx/mise-action@v4.2.5

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build for Cloudflare (OpenNext)
        run: pnpm exec opennextjs-cloudflare build

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
          # wrangler deploy never reads the runner's shell env — these must be pushed
          # as Worker secrets explicitly (via `wrangler secret bulk`), or the app 500s
          # on first Supabase/Turnstile access at runtime. NEXT_PUBLIC_* need no entry
          # here: they're already inlined into the client bundle by the build step above.
          secrets: |
            SUPABASE_URL
            SUPABASE_PUBLISHABLE_KEY
            SUPABASE_SECRET_KEY
            TURNSTILE_SECRET_KEY
